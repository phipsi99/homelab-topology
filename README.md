# homelab-topology

Public, anonymized infrastructure topology for my homelab.

The page is generated from the private homelab repo and published here automatically
on every relevant change. All security-relevant data (internal IPs, exact software
versions, CPU model, internal domains, ports, and source-config links) is masked or
removed before publishing.

Served via GitHub Pages → see the repo's **Settings → Pages**.
